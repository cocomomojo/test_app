# Issue 自動生成ガイド

## 概要

このドキュメントは、GitHub Issue を自動生成するための複数の実装方法を解説します。

GitHub Actions、CLI スクリプト、カスタムコマンドを組み合わせることで、E2E テスト作成タスクを自動化し、`@e2e-test-specialist` エージェントへの自動アサインが可能になります。

---

## 📍 ファイル配置

```
.github/
├── workflows/
│   └── auto-create-e2e-issue.yml      ← GitHub Actions ワークフロー
├── issue-templates/
│   └── e2e-test.md                    ← Issue テンプレート
├── prompts/
│   └── create-e2e-issue.prompt.md     ← カスタムコマンド
└── ...

scripts/
├── create-e2e-issue.js                ← Node.js スクリプト
└── create_e2e_issue.py                ← Python スクリプト
```

---

## 🚀 方法1: GitHub Actions でテンプレート Issue を自動生成

### 概要

GitHub Actions ワークフローを使い、**スケジュール実行** や **特定イベント（PR作成など）** をトリガーに Issue を自動作成します。

### メリット・デメリット

| 項目 | 説明 |
|------|------|
| ✅ メリット | 完全自動化、運用効率化、スケーラビリティ |
| ❌ デメリット | YAML 編集が必要、トリガーの設計に工数 |
| 推奨用途 | 定期実行、PR連携、大規模自動化 |

### Step 1: Issue テンプレートを作成

[`.github/issue-templates/e2e-test.md`]

```markdown
---
name: E2E テスト作成リクエスト
description: E2E テストを自動生成します
labels: ["test", "e2e"]
assignees: []
---

## 機能名
<!-- テスト対象の機能名 -->

## テストシナリオ
<!-- 実装するテストシナリオをチェック -->
- [ ] 正常系: 成功パターン
- [ ] 異常系: エラーハンドリング

## 受け入れ条件
<!-- テストの受け入れ条件 -->
- すべてのテストがローカルで成功すること
- Allure レポートが生成されること
```

### Step 2: GitHub Actions ワークフローを作成

#### パターンA: 定期的に Issue を自動作成（週次）

[`.github/workflows/auto-create-e2e-issue.yml`]

```yaml
name: 週次 E2E テスト Issue 自動作成

on:
  schedule:
    # 毎週月曜 09:00 UTC に実行
    - cron: '0 9 * * 1'
  workflow_dispatch:  # 手動トリガーも可能

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: E2E テスト Issue を自動作成
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "[${{ github.run_number }}] E2E テスト作成 - $(date +%Y年%m月%d日)" \
            --body "$(cat << 'EOF'
          ## 背景
          週次の定期テスト拡充タスクです。
          
          ## テストシナリオ
          - [ ] ログイン機能
          - [ ] TODO管理機能
          - [ ] メモ管理機能
          
          ## 受け入れ条件
          - すべてのテストがローカルで成功すること
          - Allureレポートが生成されること
          - テストカバレッジが80%以上であること
          EOF
          )" \
            --label "test,e2e,automated" \
            --assignee "e2e-test-specialist"
```

**実行条件：**
- 毎週月曜 09:00 UTC に自動実行
- または、GitHub UI から「Run workflow」で手動実行可能

#### パターンB: PR 作成時に Issue を自動作成

[`.github/workflows/auto-create-e2e-issue.yml`]

```yaml
name: PR 作成時に E2E テスト Issue を自動作成

on:
  pull_request:
    types: [opened]
    paths:
      - 'frontend/src/**'  # フロントエンド変更時のみ

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: PR から自動 Issue を生成
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          gh issue create \
            --title "E2E テスト: $PR_TITLE (PR #$PR_NUMBER)" \
            --body "$(cat << 'EOF'
          ## 関連 PR
          #${{ env.PR_NUMBER }}
          
          ## 対応内容に合わせた E2E テスト作成
          PR で実装した機能に対して、E2E テストを追加してください。
          
          ## 要件
          - 既存テストとの重複を避ける
          - アクセシビリティベースのセレクタを使用
          - ポジティブ・ネガティブテストを両方実装
          EOF
          )" \
            --label "test,e2e" \
            --assignee "e2e-test-specialist"
```

**トリガー：**
- PR が作成された時点で自動実行
- `frontend/src/**` の変更がある場合のみ

### 実行結果

ワークフローが実行されると、GitHub Issues に以下のような Issue が作成されます：

```
[1234] E2E テスト作成 - 2026年01月04日

## 背景
週次の定期テスト拡充タスクです。

## テストシナリオ
- [ ] ログイン機能
- [ ] TODO管理機能
- [ ] メモ管理機能

Assignee: @e2e-test-specialist
Labels: test, e2e, automated
```

---

## 🛠️ 方法2: CLI やスクリプトで Issue を作成

### 概要

GitHub CLI（`gh`）や Node.js / Python スクリプトを使用して、プログラマティックに Issue を作成します。

### メリット・デメリット

| 項目 | 説明 |
|------|------|
| ✅ メリット | 柔軟なカスタマイズ、開発時の動作確認が容易 |
| ❌ デメリット | 手動実行が基本、スケーリングに工数 |
| 推奨用途 | 手動トリガー、開発効率化、複雑なロジック |

### 方法2-A: GitHub CLI (`gh`) を使う

#### インストール

```bash
# Mac
brew install gh

# Ubuntu/Linux
sudo apt-get install gh

# Windows (Chocolatey)
choco install gh
```

#### 認証

```bash
gh auth login
# ブラウザで認証し、トークンを設定
```

#### 基本的な使い方

```bash
# Issue を作成
gh issue create \
  --title "E2E テスト作成 - $(date +%Y年%m月%d日)" \
  --body "テスト作成タスクです。" \
  --label "test,e2e" \
  --assignee "e2e-test-specialist"
```

### 方法2-B: Node.js スクリプト

#### ファイル配置

[`scripts/create-e2e-issue.js`]

```javascript
#!/usr/bin/env node

const { exec } = require('child_process');
const util = require('util');
const execPromise = util.promisify(exec);

// テスト生成要件
const testRequirements = {
  features: [
    { name: 'ログイン機能', scenarios: ['正常系', '異常系'] },
    { name: 'TODO管理', scenarios: ['作成', '削除', '完了'] },
    { name: 'メモ管理', scenarios: ['作成', '編集', '削除'] }
  ]
};

async function createE2EIssue() {
  try {
    const title = `[自動] E2E テスト作成 - ${new Date().toLocaleDateString('ja-JP')}`;
    
    const body = `
## 背景
自動生成されたE2Eテスト作成タスクです。

## 対象機能とテストシナリオ
${testRequirements.features
  .map(f => `
### ${f.name}
${f.scenarios.map(s => `- [ ] ${s}`).join('\n')}
`)
  .join('\n')}

## 受け入れ条件
- すべてのテストがローカルで成功すること
- Allureレポートが生成されること
- テストカバレッジが80%以上であること

## 実装パス
\`frontend/tests/e2e/\`

---
**自動生成**: Node.js スクリプト
`;

    const command = `gh issue create \
      --title "${title}" \
      --body "${body}" \
      --label "test,e2e,auto-generated" \
      --assignee "e2e-test-specialist"`;

    console.log('📝 Issue を作成中...');
    const { stdout } = await execPromise(command);
    
    console.log('✅ Issue を作成しました:');
    console.log(stdout);

  } catch (error) {
    console.error('❌ エラーが発生しました:', error);
    process.exit(1);
  }
}

createE2EIssue();
```

#### 実行方法

```bash
# 単独実行
node scripts/create-e2e-issue.js

# package.json に登録して実行
# package.json に以下を追加：
# "create:e2e-issue": "node scripts/create-e2e-issue.js"

npm run create:e2e-issue
```

### 方法2-C: Python スクリプト

#### ファイル配置

[`scripts/create_e2e_issue.py`]

```python
#!/usr/bin/env python3

import os
import requests
from datetime import datetime

GITHUB_TOKEN = os.getenv('GITHUB_TOKEN')
REPO_OWNER = 'cocomomojo'
REPO_NAME = 'test_app'

def create_issue():
    url = f'https://api.github.com/repos/{REPO_OWNER}/{REPO_NAME}/issues'
    
    headers = {
        'Authorization': f'token {GITHUB_TOKEN}',
        'Accept': 'application/vnd.github.v3+json'
    }
    
    today = datetime.now().strftime('%Y年%m月%d日')
    
    data = {
        'title': f'[自動] E2E テスト作成 - {today}',
        'body': '''## 背景
自動生成されたE2Eテスト作成タスクです。

## テストシナリオ
- [ ] ログイン機能のE2Eテスト
- [ ] TODO管理機能のE2Eテスト
- [ ] メモ管理機能のE2Eテスト

## 受け入れ条件
- すべてのテストがローカルで成功すること
- Allureレポートが生成されること
- テストカバレッジが80%以上であること

## 実装場所
`frontend/tests/e2e/`

---
**自動生成**: Python スクリプト
''',
        'labels': ['test', 'e2e', 'auto-generated'],
        'assignees': ['e2e-test-specialist']
    }
    
    response = requests.post(url, json=data, headers=headers)
    
    if response.status_code == 201:
        issue = response.json()
        print(f'✅ Issue を作成しました: #{issue["number"]}')
        print(f'   URL: {issue["html_url"]}')
    else:
        print(f'❌ エラー: {response.status_code}')
        print(response.json())

if __name__ == '__main__':
    create_issue()
```

#### 実行方法

```bash
export GITHUB_TOKEN=your_token_here
python3 scripts/create_e2e_issue.py
```

---

## 🤖 方法3: カスタムコマンドで Issue 生成

### 概要

GitHub Copilot のカスタムコマンドを使用して、Chat 内から Issue を生成します。

### メリット・デメリット

| 項目 | 説明 |
|------|------|
| ✅ メリット | 対話的、開発時の動作確認が容易 |
| ❌ デメリット | 手動実行が基本、大規模自動化には不向き |
| 推奨用途 | 開発時のユーザーフレンドリーなUI |

### 実装

[`.github/prompts/create-e2e-issue.prompt.md`]

```markdown
---
name: create-e2e-issue
description: E2E テスト作成の GitHub Issue を自動生成する
tools: [shell, web]
---

# GitHub Issue 自動生成コマンド

## ゴール
GitHub Issue を自動作成し、e2e-test-specialist エージェントをアサインして、
自動でE2Eテストの実装を開始させます。

## 用途
- 定期的なテスト拡充タスクの自動化
- PR に基づいたテストタスクの自動生成

## 実行方法

### GitHub CLI で Issue を作成
\`\`\`bash
gh issue create \
  --title "[テスト] $(date +%Y年%m月%d日) E2E テスト作成" \
  --body "$(cat << 'EOF'
## 背景
E2E テストを追加する必要があります。

## テストシナリオ
- [ ] 正常系のテスト
- [ ] 異常系のテスト

## 受け入れ条件
- テストがローカルで成功すること
- Allure レポートが生成されること
EOF
)" \
  --label "test,e2e" \
  --assignee "e2e-test-specialist"
\`\`\`

### 出力
Issue が自動作成され、`@e2e-test-specialist` にアサインされます。
エージェントが自動でテスト実装を開始します。
```

### 使用方法

VS Code の Copilot Chat で：

```
@create-e2e-issue

ログイン機能のE2Eテストを作成するための Issue を生成してください。
```

---

## 📊 3つの方法の比較表

| 項目 | GitHub Actions | CLI / スクリプト | カスタムコマンド |
|------|:---:|:---:|:---:|
| **自動度** | 🔴 完全自動 | 🟡 半自動 | 🟡 半自動 |
| **セットアップ難易度** | 中（YAML編集） | 低（Node.js） | 低（Prompt MD） |
| **実行頻度** | 定期実行・イベント駆動 | 手動実行 | 手動実行（Chat） |
| **推奨用途** | 定期実行・PR連携 | 開発効率化 | 対話的実行 |
| **スケーラビリティ** | ⭐⭐⭐ | ⭐⭐ | ⭐ |

---

## 🎯 実装ステップ（最小構成）

### Step 1: GitHub Actions ワークフロー追加

```bash
mkdir -p .github/workflows
```

[`.github/workflows/auto-create-e2e-issue.yml`]を作成：

```yaml
name: Auto Create E2E Issue

on:
  schedule:
    - cron: '0 9 * * 1'  # 毎週月曜 09:00 UTC

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create Issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "[自動] E2E テスト作成 $(date +%Y-%m-%d)" \
            --body "## テスト作成タスク\n\n週次のE2Eテスト拡充です。\n\n## テストシナリオ\n- [ ] ログイン機能\n- [ ] TODO管理機能\n- [ ] メモ管理機能\n\n## 受け入れ条件\n- すべてのテストがローカルで成功すること\n- Allureレポートが生成されること" \
            --label "test,e2e" \
            --assignee "e2e-test-specialist"
```

### Step 2: Git にコミット・プッシュ

```bash
git add .github/workflows/auto-create-e2e-issue.yml
git commit -m "feat: E2E Issue 自動生成ワークフローを追加"
git push origin main
```

### Step 3: 動作確認

GitHub リポジトリの **Actions** タブで、ワークフローの実行状況を確認できます。

---

## 💡 ベストプラクティス

### Issue テンプレートの設計

✅ **良い例**
```markdown
## 背景
ログイン機能の品質保証のため、E2Eテストを追加する必要があります。

## テストシナリオ
- [ ] 正常系: 正しい認証情報でログイン成功
- [ ] 異常系: 空のユーザ名でエラー表示
- [ ] 異常系: 不正なパスワードでエラー表示

## 受け入れ条件
- すべてのテストがローカルで成功すること
- Allureレポートが生成されること
```

❌ **悪い例**
```markdown
## テスト作成お願いします
```

### スケジュール設定

| 用途 | Cron 式 | 説明 |
|------|--------|------|
| 毎日 | `0 9 * * *` | 毎日 09:00 UTC |
| 毎週 | `0 9 * * 1` | 毎週月曜 09:00 UTC |
| 毎月 | `0 9 1 * *` | 毎月1日 09:00 UTC |

---

## 🔄 推奨される運用フロー

```
┌─────────────────────────────┐
│ GitHub Actions で定期生成    │
│（毎週月曜 09:00）           │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ Issue が自動作成される       │
│ (@e2e-test-specialist代入)  │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ e2e-test-specialist が      │
│ テストコード実装を開始      │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ Pull Request を自動作成     │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ コードレビュー              │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ マージ＆本番利用            │
└─────────────────────────────┘
```

---

## 📚 関連ドキュメント

- [GitHub Copilot カスタムコマンド vs カスタムエージェント比較](./12-カスタムコマンドVSカスタムエージェント比較.md)
- [E2E テスト自動生成ガイド](./13-E2Eテスト自動生成ガイド.md)
- [E2E テスト専門エージェントガイド](./14-E2Eテスト専門エージェントガイド.md)

---

## 🎯 まとめ

| 項目 | 説明 |
|------|------|
| **目的** | GitHub Issue の自動生成と e2e-test-specialist への自動アサイン |
| **3つの実装方法** | GitHub Actions、CLI/スクリプト、カスタムコマンド |
| **推奨** | GitHub Actions（定期実行）+ カスタムコマンド（手動実行） |
| **効果** | テスト作成タスクの完全自動化、開発効率化 |

このガイドに従うことで、GitHub Issue から E2E テストの自動実装までのワークフローが完全に自動化されます。

---

**作成日**: 2026年1月  
**最終更新**: 2026年1月
